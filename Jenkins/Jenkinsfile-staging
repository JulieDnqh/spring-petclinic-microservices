pipeline {
    agent any

    stages {
        stage('Test Kubeconfig on Windows') {
            steps {
                script {
                    echo "hello world"
                }
            }
        }
    }
}
