pipeline {
    agent any

    stages {
        stage('Test Kubeconfig on Windows') {
            steps {
                script {
                    bat "echo 'hello world'"
                }
            }
        }
    }
}
